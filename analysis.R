x <- seq(1,10,len = 1) #an assignment operator - assigning a value of something to a variable
y = 40*2 +rnorm (10,0,5)
plot(x,y)
summary(x) # all values are 1. 
mean(x)
sd(x)

summary (y)
mean (y)
sd(y)
barplot (y,x)

